# OCR Review Analysis

**Timestamp**: 2026-06-27T19:21:26.291353+00:00

**Original OCR Finding**:

- **File**: packages/plant_service/src/plant_service/domain/seed_packet.py
- **Lines**: 31-36
- **Type**: Comment
- **Existing Code**:
```
    def generate_id(self, seq: int = 1) -> str:
+        """
+        Generate seed packet ID in SPKT-NNN format.
+        Note: seq is passed in from the service layer which queries existing records.
+        """
+        return f"SPKT-{seq:03d}"
```

- **Suggested Code**:
```
    def generate_id(self, seq: int = 1) -> str:
+        """
+        Generate seed packet ID in SPKT-NNN format.
+        Note: seq is passed in from the service layer which queries existing records.
+        Limitation: seq must be < 1000 to maintain SPKT-NNN format.
+        """
+        if seq >= 1000:
+            raise ValueError(f"Sequence number {seq} exceeds maximum of 999 for SPKT-NNN format")
+        return f"SPKT-{seq:03d}"
```

- **Review Comment**: The generate_id method uses format 'SPKT-{seq:03d}' which produces IDs with at least 3 digits (padded with zeros). However, if seq exceeds 999, the ID will have more than 3 digits (e.g., SPKT-1000), which violates the SPKT-NNN format expectation and will fail validation in plant.py that checks for exactly 3 digits. Consider adding a check to prevent seq from exceeding 999 or document this limitation.

## LLM Analysis

**Verdict**: VALID

**Analysis**:

Confirmed real issue. plant.py:96 validates seed_packet_id with regex `^SPKT-\d{3}$` (exactly 3 digits). If seq >= 1000, generate_id produces 'SPKT-1000' which fails that validation downstream. Additionally, find_next_sequence at seed_packet.py:44 uses `^SPKT-(\d{3})$`, so IDs with 4+ digits would be invisible to sequence tracking, creating a silent loop where the same ID regenerates.

The suggestion is appropriate: a ValueError guard is the right defense-in-depth for a domain object. It fails fast at the source with a clear message rather than surfacing cryptically in Plant.create_from_dict.

Alternatives considered:
- Wider format (e.g., :04d) - breaks existing validation in plant.py and any persisted data referencing the format.
- Docstring-only - insufficient; silent format violations are worse than explicit failures.
- The suggested ValueError approach is optimal.

Testing strategy: New test only. The method is well-tested for normal paths (test_seed_packet_model.py:10-14). Add a single boundary test: `test_generate_id_rejects_seq_ge_1000` asserting ValueError for seq=1000. No need for full Red/Green - the implementation is trivial and the existing test structure is clear.

Priority: TODO(3) - low risk in practice (would require 1000+ seed packets), but the fix is 2 lines and eliminates a latent data integrity issue.

---

*Generated by review-analyzer.*
