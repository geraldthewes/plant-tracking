[project]
name = "plant-tracking-cli"
version = "0.1.0"
description = "CLI for plant tracking with QR label generation"
authors = [{name = "Gardener", email = "gardener@example.com"}]
dependencies = [
    "qrcode[pil]>=7.4",
    "Pillow>=10.0.0",
    "PyYAML>=6.0",
    "pyusb>=1.2.0",
    "thefuzz[speedup]>=0.22.0",
]

[project.optional-dependencies]
test = [
    "pytest>=7.0.0",
]

[project.scripts]
plant-tracking = "commands.plant_tracking_cli:main"

[tool.hatch.build.targets.wheel]
packages = ["commands"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
