# OCR Review Analysis

**Timestamp**: 2026-06-15T12:36:54.064719+00:00

**Original OCR Finding**:

- **File**: packages/plant_service/src/plant_service/adapters/repository/plant_repository.py
- **Lines**: 0-0
- **Type**: Comment
- **Existing Code**:
```
    def update_plant(self, plant_id: str, plant_data: dict) -> PlantDomain | None:
        """Update an existing plant"""
        orm_obj = self.get(plant_id)
        if not orm_obj:
            return None

        for key, value in plant_data.items():
            if hasattr(orm_obj, key) and key not in ("id",):
                setattr(orm_obj, key, value)

        self.update(orm_obj)
        return orm_obj.to_domain()
```

- **Suggested Code**:
```
    def update_plant(self, plant_id: str, plant_data: dict) -> PlantDomain | None:
        """Update an existing plant"""
        orm_obj = self.get(plant_id)
        if not orm_obj:
            return None

        # Define fields that should not be updated via this method
        protected_fields = {"id", "created_at", "updated_at"}
        
        for key, value in plant_data.items():
            if hasattr(orm_obj, key) and key not in protected_fields:
                setattr(orm_obj, key, value)

        self.update(orm_obj)
        return orm_obj.to_domain()
```

- **Review Comment**: The update_plant method updates any attribute from plant_data on the ORM object (except 'id') without validating if the attribute is safe to update. This risks unintended modification of fields like timestamps (created_at, updated_at) or foreign keys that should not be changed directly. Consider implementing a whitelist of allowed fields for update or explicitly protecting sensitive fields.

## LLM Analysis

**Verdict**: VALID

**Analysis**:

1. CONFIRMED: This is a real problem. The `update_plant` method iterates over arbitrary keys from `plant_data` and sets them via `setattr`, only excluding `"id"`. The `Plant` ORM model inherits `TimestampMixin` (`base.py:12-18`), which adds `created_at` and `updated_at` columns. A malicious or buggy caller passing `{"created_at": "some-date"}` would overwrite the creation timestamp, corrupting audit data.

2. WHY PROBLEMATIC: Two concrete risks: (a) `created_at` can be silently overwritten to any value, destroying the record's creation audit trail. (b) Foreign keys `genus_id` and `seed_packet_id` (`plant_model.py:32-37`) can be changed directly, bypassing domain logic that should validate referential integrity and ownership. The `onupdate=func.now()` on `updated_at` mitigates that specific field partially (SQLAlchemy will re-set it on flush), but `created_at` has no such protection.

3. CRITIQUE OF SUGGESTION: The proposed blacklist fix is correct and sufficient for the immediate risk. However, blacklists are inherently fragile — adding a new protected field in the future requires remembering to update this set. The reviewer's own recommendation of a whitelist is actually the stronger approach.

4. BETTER ALTERNATIVE: A whitelist aligned with the explicit field list in `create_plant` (`plant_repository.py:37-51`) would be more robust:
```python
UPDATABLE_FIELDS = {
    "variety_name", "latin_name", "brand",
    "days_to_maturity", "germination_time",
    "planting_depth", "spacing", "sun_requirements",
    "indoor_start_time", "planting_date",
}
```
This fails closed: new fields added to the model won't accidentally become updatable. Additionally, `genus_id` and `seed_packet_id` should arguably be excluded even from the whitelist, as relationship changes should go through dedicated domain methods.

5. TESTING STRATEGY: Red/Green TDD is appropriate here, because there are ZERO existing tests for `update_plant` in the integration test suite (`test_repository.py` has no update tests). Two test cases: (a) verify protected fields (`created_at`, `id`) are not modified when included in `plant_data`, (b) verify legitimate fields are updated correctly.

6. VERDICT: VALID — merge the suggested fix as a minimum, but prefer the whitelist approach for defense-in-depth. The `update_plant` method has no integration test coverage, so tests are mandatory before merging.

---

*Generated by review-analyzer.*
