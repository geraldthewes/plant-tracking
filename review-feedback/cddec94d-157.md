# OCR Review Analysis

**Timestamp**: 2026-06-15T12:36:54.067206+00:00

**Original OCR Finding**:

- **File**: packages/plant_service/src/plant_service/service_layer/export_service.py
- **Lines**: 137-146
- **Type**: Comment
- **Existing Code**:
```
    @staticmethod
    def _write_markdown_file(filepath: Path, data: dict) -> None:
        """Write a single record as a Markdown file with YAML frontmatter."""
        lines = ["---"]
        for key, value in data.items():
            if value is not None:
                lines.append(f"{key}: {value}")
        lines.append("---")
        lines.append("")
        filepath.write_text("\n".join(lines))
```

- **Suggested Code**:
```
    @staticmethod
    def _write_markdown_file(filepath: Path, data: dict) -> None:
        """Write a single record as a Markdown file with YAML frontmatter."""
        import yaml
        lines = ["---"]
        # Convert all values to strings and handle None values
        cleaned_data = {}
        for key, value in data.items():
            if value is not None:
                # Convert to string for consistent YAML frontmatter
                cleaned_data[key] = str(value)
        # Use yaml.dump to ensure proper YAML formatting
        yaml_content = yaml.dump(cleaned_data, default_flow_style=False)
        lines.append(yaml_content)
        lines.append("---")
        lines.append("")
        filepath.write_text("\n".join(lines))
```

- **Review Comment**: The _write_markdown_file method writes field values directly without ensuring they are properly formatted for YAML. While integer fields are fine, string fields containing special YAML characters (like colons, quotes, etc.) or values that look like YAML literals (e.g., 'yes', 'no', 'true', 'false', numbers) may produce invalid YAML. Additionally, the PlantLogEntry model includes integer fields (id, level, amount_ml) that are written as unquoted integers, which is valid YAML but may be unexpected in Markdown frontmatter where string values are typical. Consider converting all values to strings and ensuring proper YAML escaping, or using a YAML library to generate the frontmatter.

## LLM Analysis

**Verdict**: VALID

**Analysis**:

The issue is real and demonstrable. The current _write_markdown_file uses naive f-string interpolation (f'{key}: {value}') to produce YAML frontmatter, which breaks in three concrete ways:

1. **Colons in values** (e.g., variety_name = 'Tomato: Cherry Type') produce invalid YAML: 'variety_name: Tomato: Cherry Type' → parse error 'mapping values are not allowed here'.
2. **YAML-reserved strings** ('yes', 'no', 'true', 'false') are silently coerced to booleans by YAML parsers, corrupting data on round-trip.
3. **Multiline values** (PlantLogEntry.text supports markdown per docstring at plant_log.py:15) would split across lines and break frontmatter parsing entirely.

The suggested fix direction is correct but has two flaws:
- **`import yaml` inside the function**: should be a module-level import. The project already depends on PyYAML 6.0.3.
- **Converting all values to `str()`**: unnecessary. `yaml.safe_dump()` handles mixed types correctly. Forcing strings adds superfluous quoting (e.g., `'42'` instead of `42`).

Better fix: use `yaml.safe_dump(data, default_flow_style=False)` with module-level import, no str() conversion. The `text` field with newlines is the most likely real-world trigger, as user notes can contain markdown formatting.

Testing strategy: **Red/Green TDD**. Write tests that feed problematic values (colons, booleans, multiline text, reserved words) to _write_markdown_file, verify they fail, then fix. No existing export tests exist (glob found zero matches for test_export*). Minimum 4 test cases: colon in value, reserved word, multiline text, and the happy path.

---

*Generated by review-analyzer.*
